{% extends 'base.html.twig' %}

{% block title %}Docteur{% endblock %}

{% block body %}
    <h1>Docteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ docteur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('docteur_index') }}">back to list</a>

    <a href="{{ path('docteur_edit', {'id': docteur.id}) }}">edit</a>

    {{ include('docteur/_delete_form.html.twig') }}
{% endblock %}
