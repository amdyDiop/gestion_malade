{% extends 'base.html.twig' %}

{% block title %}TypeVisite{% endblock %}

{% block body %}
    <h1>TypeVisite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_visite.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_visite.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_visite_index') }}">back to list</a>

    <a href="{{ path('type_visite_edit', {'id': type_visite.id}) }}">edit</a>

    {{ include('type_visite/_delete_form.html.twig') }}
{% endblock %}
