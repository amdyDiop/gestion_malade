{% extends 'base.html.twig' %}

{% block title %}Infirmier{% endblock %}

{% block body %}
    <h1>Infirmier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ infirmier.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('infirmier_index') }}">back to list</a>

    <a href="{{ path('infirmier_edit', {'id': infirmier.id}) }}">edit</a>

    {{ include('infirmier/_delete_form.html.twig') }}
{% endblock %}
