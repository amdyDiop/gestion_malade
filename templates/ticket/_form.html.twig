{{ form_start(form) }}
<div class="card">
    <div class="card-body">
        <div class="row">
            <div class="form-group col-md-6">
                {{ form_row(form.montant) }}
            </div>
            <div class="form-group col-md-6">
                {{ form_row(form.typeVisite) }}
            </div>
        </div>
        <div class="row">
            <div class="form-group col-md-6">
                <label class="label label-info">Ajouter le type de visite s'il n'existe pas </label>
               <input type="text" name="typeVisite" class="form-control">
            </div>
        </div>
    </div>
    <div class="card-footer">
        <button class="btn btn-primary pull-right">Enregistrer</button>
    </div>
</div>
{{ form_end(form) }}
