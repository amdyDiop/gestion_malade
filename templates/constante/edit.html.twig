{% extends 'base.html.twig' %}

{% block title %}Edit Constante{% endblock %}

{% block body %}
    <h1>Edit Constante</h1>

    {{ include('constante/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('constante_index') }}">back to list</a>

    {{ include('constante/_delete_form.html.twig') }}
{% endblock %}
